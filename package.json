{
  "name": "puzzle-forge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run test && tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "tsc --project tsconfig.test.json && node --test .test-build/tests/**/*.test.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "preact": "^10.26.9"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vite": "^7.0.0"
  }
}
